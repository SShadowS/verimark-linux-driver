# Proprietary vendor artifacts and secrets — NEVER commit / redistribute.
re/
captures/
ghidra-projects/
*.pcapng
*.pcap
*.sys
*.dll
*.exe
*.cab
*.key
# Host pairing identity (contains the host EC private key) — local secret, never commit.
prototype/pdata/
# Extracted Windows OWNER pairing material (owner EC private key) — local secret, never commit.
pairing-fields.json
plain.bin
# Local Python venv (cryptography/pyusb for the prototype).
.venv/
# Local Claude Code machine config (permission allowlist + local paths) — not for publish.
.claude/
# Full decrypted command transcripts (embed the Windows user SID) — local, never commit.
reference/protocol/transcripts/
# Raw byte dump extracted from the proprietary DLL (regenerable from Ghidra) — treat like re/.
prototype/p256_params.bin
prototype/images/

# keep the placeholders
!re/.gitkeep
!captures/.gitkeep
!.gitignore

# Python bytecode
__pycache__/
*.pyc
prototype/prints/
driver/tests/build/
